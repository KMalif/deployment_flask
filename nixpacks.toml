[phases.setup]
nixPkgs = ["python38", "postgresql", "pipenv", "gcc", "libgl1-mesa-glx"]

[phases.install]
cmd = "python -m venv --copies /opt/venv && ./otp/venv/bin/activate && pip install -r requirements.txt"

[phases.start]
cmd = "gunicorn run:app"